# Chrome Web Store listing

Everything the submission form asks for, ready to paste. Screenshots are
generated by `tools/make-store-shots.py`.

---

## Name

```
FullShot - Open Source Full Page Screenshot
```

Renamed 28 August 2026. The old name was the bare word "FullShot", which loses
in search to every rival: seven other extensions carry that name, and six of
them append the phrase people actually type. "Open source" leads because it is
the one differentiator a clone cannot copy without publishing its own code.

Renaming resets the review queue, so send it together with anything else that is
pending.

WHERE THESE TWO FIELDS ACTUALLY LIVE: the store takes the item name and the
one-line summary under the title from `manifest.json`, not from the dashboard.
There is no name field in the developer console to edit. So the rename and the
new short description both require editing `name` and `description` in the
manifest, bumping the version, and uploading a new package. Only the long
Detailed description below is a dashboard field you can change on its own.

## Short description

Max 132 characters. This one is 122.

```
Open source full page screenshot. Scrolls, stitches, exports PNG, JPG or PDF. 33 KB, no account, no servers, no telemetry.
```

## Category

Workflow & Planning

## Detailed description

Paste this WHOLE block into the form. As of 27 August 2026 the live listing
carried only the short description, two sentences, against rivals running four
to six hundred words. That, not the name, is the biggest thing costing installs.

```
FullShot captures a whole web page, not just the part you can see, and saves it
as a PNG, JPG or PDF.

HOW IT WORKS

Chrome will not let an extension capture anything outside the visible viewport,
so FullShot scrolls the page one screen at a time, takes a shot at each stop,
and stitches them into a single image. Fixed headers are hidden after the first
slice so they do not repeat down the screenshot, and the page is walked once
beforehand to trigger lazy-loaded images.

Web apps are the hard case, and most tools fail it. Gmail, Notion, Slack and
dashboards do not scroll the page: the content moves inside a panel while the
page itself measures exactly one screen. Point another tool at your inbox and
you get one screen and nothing more. FullShot finds that panel, scrolls it, and
crops every slice to it, so the sidebar, the app header and the buttons pinned
to the panel do not repeat down the image.

TWO WAYS TO CAPTURE

• Full page (Alt+Shift+P): scrolls and stitches the whole thing.
• Selected area (Alt+Shift+S): drag over what you want. No scrolling, because
  what you selected is already on screen.

IN THE VIEWER

• Name the file before saving, or leave it to be named after the page and date
• Download as PNG, JPG or PDF
• Copy straight to the clipboard, to paste into a message or a document
• Crop the capture by dragging over the preview
• Capture again without going back to the tab

33 KB, AND YOU CAN READ ALL OF IT

The whole extension is 33 KB. Every other full-page screenshot extension we
compared it against sits between 93 KB and 366 KB.

It is small because there is nothing in it but the feature. No bundled
libraries, not even for the PDF: that file is written by under a hundred lines
of our own code. Nothing is fetched from a CDN while it runs, so what Google
reviewed is exactly what runs on your machine.

It is free and open source under the MIT license, and all of it is on GitHub at
github.com/tooltank/fullshot. If you want to know what it does with your page,
you can go read it in an afternoon.

NOTHING LEAVES YOUR COMPUTER

There is no account, no sign-in, no plan, and no server. The capture is built in
your browser and stays there. FullShot does not collect, transmit or sell any
data, it has no analytics of any kind, and it makes no network requests at all.

It asks for access to one tab at a time, only when you start a capture, and that
access ends when the tab navigates away or closes. It never asks for standing
access to any site. Here are the five permissions it uses and what each is for:

• activeTab: the one tab you invoked it on, and only after you invoke it.
• scripting: injects the scroll-and-capture script when a capture starts.
• downloads: saves the file you asked for, in the format you chose.
• storage: holds the slices between capture and viewer, plus your settings.
• unlimitedStorage: a long page makes tens of megabytes of slices before they
  are stitched. Without this, long captures fail halfway.

WHAT IT DOES NOT DO

Better you know before you install it.

• There is no annotation editor. No arrows, no text, no blur. If you need to
  mark up a screenshot, this is not your tool.
• It does not work on chrome:// pages or on the Chrome Web Store itself. That is
  a Chrome restriction on every extension, not a fault of this one.
• Very tall pages are scaled down to fit what a browser canvas can hold, and the
  viewer tells you when that happened.
```

---

## Permission justifications

One box per permission in the submission form. These describe what the code
actually does, which is what the reviewer checks against.

### activeTab

```
Used to capture the tab the user invokes the extension on, either by clicking
the toolbar icon or pressing the keyboard shortcut. That gesture is what grants
access; the extension has no access to any tab before it, and the grant ends
when the tab navigates or closes. This is what allows chrome.tabs.captureVisibleTab()
to run on that tab. No other tab is ever read.
```

### scripting

```
Used to inject page.js into the active tab on demand when a capture starts. That
script scrolls the page so each section can be captured, hides scrollbars and
disables smooth scrolling so the sections line up, hides position:fixed and
sticky elements so headers do not repeat down the screenshot, and draws the
selection overlay in area-capture mode. It is injected only when a capture
begins and it is never registered as a persistent content script.
```

### downloads

```
Used to save the finished capture when the user clicks Download. The extension
writes only the file the user asked for, in the format they chose. It does not
read the download history or touch any other file.
```

### storage

```
Two uses, both chrome.storage.local. It holds the captured sections between the
service worker and the viewer tab, because the image data cannot be passed
directly between them; the viewer deletes them as soon as it has read them. It
also holds three user preferences: the JPEG quality, the file name format, and
whether fixed elements are hidden during capture. Nothing is synced to any
account. No browsing data or page content is stored.
```

### unlimitedStorage

```
A full-page capture of a long page produces sections that can total tens of
megabytes before they are stitched together. Without this permission, the
default storage quota is exceeded and long pages fail mid-capture. The data is
temporary and is deleted the moment the viewer reads it, and captures older than
one hour are cleared on startup.
```

---

## Data usage disclosure

Tick **Website content**. Leave every other box unticked.

Do NOT tick "nothing collected" here. Chrome defines handling user data broadly
— collecting, transmitting, using or sharing — and the captured image IS page
content the extension works with. More to the point: the privacy policy states
plainly that the extension processes the content of the page you capture. If
this form claimed nothing is handled, the form and the policy would contradict
each other, and that inconsistency is what a reviewer flags. Declaring it with
an honest explanation costs nothing; omitting it is what gets items rejected.

There is NO free-text field here. The data usage section is checkboxes only:
one group to disclose the data types, a second group for the certifications.
Tick Website content and move on — do not go hunting for a box to explain it in.

The explanation lives in the privacy policy, and this is the sentence that
carries it there (already published on the policy page, no action needed):

> The extension captures an image of the page the user explicitly chooses to
> screenshot. That image is processed entirely on the user's device and is never
> transmitted anywhere. The extension makes no network requests of any kind.

That is exactly why the policy URL matters: the checkbox says WHAT is handled,
the policy says HOW. The form has no room for the second half.

Not ticked, and all true: personally identifiable information, health
information, financial information, authentication information, personal
communications, location, web history, user activity.

## Remote code

Select **"No, I am not using remote code"**. Everything ships inside the
package and there are no third-party libraries at all: the PDF is written by
`minipdf.js`, which is ours. No CDN, no eval, no remotely hosted script.

This wording matters. The earlier package bundled jsPDF, whose minified file
contains a cdnjs URL, and that alone is remote code as far as review is
concerned. Do not describe the package as "vendored in `lib/`" again.

## Certifications

Certifications to tick:

- Does not sell or transfer user data to third parties, outside of approved use cases
- Does not use or transfer user data for purposes unrelated to the item's single purpose
- Does not use or transfer user data to determine creditworthiness or for lending purposes

## Single purpose

```
FullShot captures a web page as an image: either its full length, scrolled and
stitched into a single image, or a region the user selects. The result opens in
a preview tab where the user saves it locally as PNG, JPG or PDF. That is its
only function.
```

## Privacy policy URL

Required because the extension handles user data (the captured page content),
not merely because the form asks. It must point DIRECTLY at a page whose content
is the policy — a repository, a homepage, a Google Doc or anything behind a login
gets rejected. This is what the first submission was rejected for.

```
https://tooltank.io/fullshot/privacidad/
```

Sin `www` y con la barra final: el sitio redirige www → apex de forma permanente,
y un enlace que redirige es justo lo que puede volver a marcarse.

La página está en inglés por defecto (el revisor lee en inglés) con la versión en
español debajo. Fuente: `ToolTank\Website\fullshot\privacidad\index.html`.
La copia suelta en `My Business\fullshot-privacy\` quedó obsoleta.

---

## Screenshots

`tools/make-store-shots.py` renders them at 1280x800, the size the store wants:

1. `01-vista-previa.png` — the viewer with a full capture
2. `02-menu-descarga.png` — the download menu open on PNG, JPG, PDF
3. `03-ajustes.png` — the settings panel

Regenerate with:

```
python tools/make-store-shots.py
```

## Promotional tiles

Both are generated by the same script, sharing one layout scaled up:

```
python tools/make-promo-tile.py
```

- `store-assets/promo-tile-440x280.png` — small tile, shown on the listing
- `store-assets/promo-marquee-1400x560.png` — marquee, only used if Google
  features the extension, but the form asks for it

## Store icon

`icons/icon128.png`, already the right size.
